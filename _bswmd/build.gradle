// workaround for TAR-85183

plugins {
    id 'com.vector.cfg.pai.automation-build-gradle'
}

scriptProject {
    cfgPath = file(cfg6)
    bswmd {
        bswPath = file("$localSipStore/$localSip")
        isActivated = true
    }
}
