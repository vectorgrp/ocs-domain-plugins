// Handle BSWMD dependencies

plugins {
    id 'com.vector.cfg.pai.automation-build-gradle'
}

scriptProject {
    cfgPath = file(cfg6)
    bswmd {
        bswPath = file("$localSipStore/$localSip")
        isActivated = true
    }
}
